const Item = require("../models/Item");
const Category = require("../models/Category");
const async = require("async");

exports.getCategoryList = (req, res, next) => {
  res.send("GET for /categories. Will add later.");
};
