extends layout 

block content 
  h1= title 

  if item
    h2= item.name 
    p #[strong Stock Code:] #{item.stockCode}
    p= item.description
    a(href=item.category.url) #{item.category.name}
    br
    p #[strong Price:] $#{item.price}

  else 
    p Somethings wrong 